/**
 * Initial Firestore seed payloads only — not used by UI at runtime.
 */
export { mockTables as SEED_TABLES } from './mock-tables';
export { mockMenuItems as SEED_MENU_ITEMS } from './mock-menu';
export { mockOrders as SEED_ORDERS } from './mock-orders';
export { MOCK_USERS as SEED_USERS, MOCK_ROLES as SEED_ROLES } from './mock-users';
